let canvas = document.getElementById("frac");
canvas.width = 680;
canvas.height = 680;

frac = canvas.getContext("2d");

for (x = 0; x < 680; x++) {
  for (y = 0; y < 680; y++) {
    // mandelbrot(x,y);
  }
}

// function mandelbrot(x,y) {}
